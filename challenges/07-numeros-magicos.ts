// Números mágicos

const INTERVAL_30_MINUTES = 60 * 30 * 1000;

function lookForUpdades() {}

// created a variable named with the amount of time and also send one with a comment
setInterval(lookForUpdades, 60 * 30 * 1000) // 30 minutes
setInterval(lookForUpdades, INTERVAL_30_MINUTES)

//  changed variables names to show the currency and percentage
function calculateDiscount(priceInCents, discountInPercentage) {}